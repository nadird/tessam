
<div class="form">
    {{ form_start(documentForm) }}
    <div class=" row g-3">
        <div class="col-md-6">
            {{ form_label(documentForm.name) }}
            {{ form_widget(documentForm.name) }}
        </div>
        <div class="col-md-6">
            {{ form_widget(documentForm.url) }}
        </div>
        <div class="col-md-6">
            {{ form_widget(documentForm.type) }}
        </div>
        <div class="col-md-6">
            {{ form_widget(documentForm.submit) }}
        </div>
    </div>

    {{ form_end(form) }}

</div>
