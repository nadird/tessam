<form method="post" action="{{ path('aapp_dashboard_webmanagement_menu_item_delete', {'id': web_menu_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ web_menu_item.id) }}">
    <button class="btn">Delete</button>
</form>
