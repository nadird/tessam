{% extends 'dashboard/base_dashboard.html.twig' %}

{% block body %}
    {% include('dashboard/_partials/_dashboard_page_header.html.twig') %}

    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="d-flex justify-content-between">
                    <h3>{{ 'All Categories'|trans }}</h3>
                    <a href="#" class="btn btn-phoenix-primary">
                        <i class="fas fa-plus-circle"></i>
                        {{ 'category.new'|trans }}
                    </a>
                </div>
            </div>
            <div class="col-md-12">
                {% include('dashboard/Catalog/Category/_partials/_filter_form.html.twig') %}
            </div>
            <div class="col-md-12">
                {% include('dashboard/Catalog/Category/_partials/_category_list.html.twig') %}
            </div>

        </div>
    </div>
{% endblock %}