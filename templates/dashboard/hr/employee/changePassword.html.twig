{% extends 'dashboard/base_dashboard.html.twig' %}

{% block title %}Hello DashboardHomeController!{% endblock %}

{% block body %}
    <h1>{{ 'Hello ' | trans }} {{employee.firstname}}</h1>
    <div class="card">
        <div class="card-header">
            <h3> {{ 'Change password for' | trans }} {{ employee.firstname }}</h3>
        </div>
        <div class="card-body">

            {{ form(passwordForm) }}
        </div>
    </div>

{% endblock %}
