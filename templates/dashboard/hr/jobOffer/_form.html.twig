<div class="card">
    <div class="card-body">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-outline-phoenix-warning">
            <i class="fa fa-save"></i>
            {{ button_label|default('Save') }}</button>
        {{ form_end(form) }}
    </div>
</div>

