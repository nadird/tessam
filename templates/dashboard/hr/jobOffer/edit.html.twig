{% extends 'dashboard/base_dashboard.html.twig' %}


{% block body %}
    <h1>{{ 'Edit WebJobOffer' | trans }}</h1>

    {{ include('dashboard/hr/jobOffer/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="d-flex flex-between-end">
        <a href="{{ path('app_dashboard_hr_job_offer_index') }}" class="btn btn-outline-phoenix-info">
            <i class="fa arrow-left"></i>
            {{ 'back to list' | trans }}</a>
        {{ include('dashboard/hr/jobOffer/_delete_form.html.twig') }}
    </div>

{% endblock %}
