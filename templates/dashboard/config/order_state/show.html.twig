{% extends 'base.html.twig' %}

{% block title %}OrderState{% endblock %}

{% block body %}
    <h1>OrderState</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order_state.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ order_state.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ order_state.code }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ order_state.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_order_state_index') }}">back to list</a>

    <a href="{{ path('app_order_state_edit', {'id': order_state.id}) }}">edit</a>

    {{ include('order_state/_delete_form.html.twig') }}
{% endblock %}
