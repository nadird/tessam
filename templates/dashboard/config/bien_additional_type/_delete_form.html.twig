<form method="post" action="{{ path('app_bien_additional_type_delete', {'id': bien_additional_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bien_additional_type.id) }}">
    <button class="btn">Delete</button>
</form>
