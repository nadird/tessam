{% extends 'base.html.twig' %}

{% block title %}BankCreditState{% endblock %}

{% block body %}
    <h1>BankCreditState</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bank_credit_state.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bank_credit_state.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ bank_credit_state.code }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ bank_credit_state.color }}</td>
            </tr>
            <tr>
                <th>IsValid</th>
                <td>{{ bank_credit_state.isValid ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bank_credit_state_index') }}">back to list</a>

    <a href="{{ path('app_bank_credit_state_edit', {'id': bank_credit_state.id}) }}">edit</a>

    {{ include('bank_credit_state/_delete_form.html.twig') }}
{% endblock %}
