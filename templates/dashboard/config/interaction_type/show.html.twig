{% extends 'base.html.twig' %}

{% block title %}InteractionType{% endblock %}

{% block body %}
    <h1>InteractionType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ interaction_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ interaction_type.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ interaction_type.code }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ interaction_type.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_interaction_type_index') }}">back to list</a>

    <a href="{{ path('app_interaction_type_edit', {'id': interaction_type.id}) }}">edit</a>

    {{ include('interaction_type/_delete_form.html.twig') }}
{% endblock %}
