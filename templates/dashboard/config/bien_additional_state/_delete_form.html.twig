<form method="post" action="{{ path('app_bien_additional_state_delete', {'id': bien_additional_state.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bien_additional_state.id) }}">
    <button class="btn">Delete</button>
</form>
