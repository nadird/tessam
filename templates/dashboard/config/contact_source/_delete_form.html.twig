<form method="post" action="{{ path('app_dashboard_config_contactSource_delete', {'id': contact_source.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact_source.id) }}">
    <button class="btn btn-phoenix-secondary">Delete</button>
</form>
