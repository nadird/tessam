{% extends 'base.html.twig' %}

{% block title %}ContactSource{% endblock %}

{% block body %}
    <h1>ContactSource</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact_source.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ contact_source.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contact_source_index') }}">back to list</a>

    <a href="{{ path('app_contact_source_edit', {'id': contact_source.id}) }}">edit</a>

    {{ include('contact_source/_delete_form.html.twig') }}
{% endblock %}
