{% extends 'base.html.twig' %}

{% block title %}NotificationType{% endblock %}

{% block body %}
    <h1>NotificationType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ notification_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ notification_type.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ notification_type.code }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ notification_type.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_notification_type_index') }}">back to list</a>

    <a href="{{ path('app_notification_type_edit', {'id': notification_type.id}) }}">edit</a>

    {{ include('notification_type/_delete_form.html.twig') }}
{% endblock %}
