{% extends 'base.html.twig' %}

{% block title %}LeadState{% endblock %}

{% block body %}
    <h1>LeadState</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lead_state.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ lead_state.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ lead_state.code }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ lead_state.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lead_state_index') }}">back to list</a>

    <a href="{{ path('app_lead_state_edit', {'id': lead_state.id}) }}">edit</a>

    {{ include('lead_state/_delete_form.html.twig') }}
{% endblock %}
