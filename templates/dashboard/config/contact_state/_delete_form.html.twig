<form method="post" action="{{ path('app_dashboard_config_contactState_delete', {'id': contact_state.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact_state.id) }}">
    <button class="btn badge-phoenix-warning">Delete</button>
</form>
