{% extends 'base.html.twig' %}

{% block title %}ContactState{% endblock %}

{% block body %}
    <h1>ContactState</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact_state.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ contact_state.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ contact_state.code }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ contact_state.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contact_state_index') }}">back to list</a>

    <a href="{{ path('app_contact_state_edit', {'id': contact_state.id}) }}">edit</a>

    {{ include('dashboard/config/contact_state/_delete_form.html.twig') }}
{% endblock %}
