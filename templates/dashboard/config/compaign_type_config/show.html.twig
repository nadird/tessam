{% extends 'base.html.twig' %}

{% block title %}CompaignType{% endblock %}

{% block body %}
    <h1>CompaignType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compaign_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ compaign_type.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ compaign_type.code }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ compaign_type.color }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ compaign_type.logo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_compaign_type_config_index') }}">back to list</a>

    <a href="{{ path('app_compaign_type_config_edit', {'id': compaign_type.id}) }}">edit</a>

    {{ include('compaign_type_config/_delete_form.html.twig') }}
{% endblock %}
