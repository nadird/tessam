<form method="post" action="{{ path('app_dashboard_config_mouvementCompte_type_delete', {'id': mouvement_compte_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mouvement_compte_type.id) }}">
    <button class="btn btn-phoenix-primary">Delete</button>
</form>
