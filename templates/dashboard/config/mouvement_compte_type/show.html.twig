{% extends 'base.html.twig' %}

{% block title %}MouvementCompteType{% endblock %}

{% block body %}
    <h1>MouvementCompteType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mouvement_compte_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mouvement_compte_type.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ mouvement_compte_type.code }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ mouvement_compte_type.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mouvement_compte_type_index') }}">back to list</a>

    <a href="{{ path('app_mouvement_compte_type_edit', {'id': mouvement_compte_type.id}) }}">edit</a>

    {{ include('mouvement_compte_type/_delete_form.html.twig') }}
{% endblock %}
