{% extends 'base.html.twig' %}

{% block title %}Wilaya{% endblock %}

{% block body %}
    <h1>Wilaya</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ wilaya.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ wilaya.name }}</td>
            </tr>
            <tr>
                <th>IsoCode</th>
                <td>{{ wilaya.isoCode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dashboard_wilaya_index') }}">back to list</a>

    <a href="{{ path('app_dashboard_wilaya_edit', {'id': wilaya.id}) }}">edit</a>

    {{ include('dashboard/config/wilaya/_delete_form.html.twig') }}
{% endblock %}
