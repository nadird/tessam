{% extends 'base.html.twig' %}

{% block title %}ChargeType{% endblock %}

{% block body %}
    <h1>ChargeType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ charge_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ charge_type.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ charge_type.code }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ charge_type.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_charge_type_index') }}">back to list</a>

    <a href="{{ path('app_charge_type_edit', {'id': charge_type.id}) }}">edit</a>

    {{ include('charge_type/_delete_form.html.twig') }}
{% endblock %}
