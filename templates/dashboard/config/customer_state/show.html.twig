{% extends 'base.html.twig' %}

{% block title %}CustomerState{% endblock %}

{% block body %}
    <h1>CustomerState</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ customer_state.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ customer_state.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ customer_state.code }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ customer_state.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_customer_state_index') }}">back to list</a>

    <a href="{{ path('app_customer_state_edit', {'id': customer_state.id}) }}">edit</a>

    {{ include('customer_state/_delete_form.html.twig') }}
{% endblock %}
