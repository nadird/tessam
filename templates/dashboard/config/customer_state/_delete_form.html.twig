<form method="post" action="{{ path('app_dashboard_config_customerState_delete', {'id': customer_state.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ customer_state.id) }}">
    <button class="btn btn-phoenix-primary">Delete</button>
</form>
