{% extends 'base.html.twig' %}

{% block title %}CProjectState{% endblock %}

{% block body %}
    <h1>CProjectState</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ c_project_state.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ c_project_state.name }}</td>
            </tr>
            <tr>
                <th>Percentage</th>
                <td>{{ c_project_state.percentage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_c_project_state_index') }}">back to list</a>

    <a href="{{ path('app_c_project_state_edit', {'id': c_project_state.id}) }}">edit</a>

    {{ include('c_project_state/_delete_form.html.twig') }}
{% endblock %}
