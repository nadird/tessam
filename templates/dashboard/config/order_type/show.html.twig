{% extends 'base.html.twig' %}

{% block title %}OrderType{% endblock %}

{% block body %}
    <h1>OrderType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ order_type.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ order_type.code }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ order_type.color }}</td>
            </tr>
            <tr>
                <th>TextColor</th>
                <td>{{ order_type.textColor }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dashboard_config_orderType_index') }}">back to list</a>

    <a href="{{ path('app_dashboard_config_orderType_edit', {'id': order_type.id}) }}">edit</a>

    {{ include('dashboard/config/order_type/_delete_form.html.twig') }}
{% endblock %}
