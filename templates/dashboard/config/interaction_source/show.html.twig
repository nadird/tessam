{% extends 'base.html.twig' %}

{% block title %}InteractionSource{% endblock %}

{% block body %}
    <h1>InteractionSource</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ interaction_source.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ interaction_source.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ interaction_source.code }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ interaction_source.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_interaction_source_index') }}">back to list</a>

    <a href="{{ path('app_interaction_source_edit', {'id': interaction_source.id}) }}">edit</a>

    {{ include('interaction_source/_delete_form.html.twig') }}
{% endblock %}
