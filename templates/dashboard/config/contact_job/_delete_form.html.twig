<form method="post" action="{{ path('app_dashboard_config_contactJob_delete', {'id': contact_job.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact_job.id) }}">
    <button class="btn btn-phoenix-primary">Delete</button>
</form>
