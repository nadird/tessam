{% extends 'base.html.twig' %}

{% block title %}TaskStatus{% endblock %}

{% block body %}
    <h1>TaskStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ task_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ task_status.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ task_status.code }}</td>
            </tr>
            <tr>
                <th>BgColor</th>
                <td>{{ task_status.bgColor }}</td>
            </tr>
            <tr>
                <th>TextColor</th>
                <td>{{ task_status.textColor }}</td>
            </tr>
            <tr>
                <th>BorderColor</th>
                <td>{{ task_status.borderColor }}</td>
            </tr>
            <tr>
                <th>IsFinal</th>
                <td>{{ task_status.isFinal ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_task_status_index') }}">back to list</a>

    <a href="{{ path('app_task_status_edit', {'id': task_status.id}) }}">edit</a>

    {{ include('task_status/_delete_form.html.twig') }}
{% endblock %}
