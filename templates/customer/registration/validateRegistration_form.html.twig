{% extends 'customer/base_customer.html.twig' %}

{% block title %}{{ 'Validate Registration account' | trans }}{% endblock %}

{% block body %}
    <div class="main">
        <div class="container mt-3 mt-md-7">
            <div class="row">
                <div class="col-12 col-md-6">
                    <div class="card">
                        <div class="card-header">
                            <h2>
                            {{ 'Registration form' | trans }}
                            </h2>
                        </div>
                        <div class="card-body">
                            {{ form(registrationForm) }}
                        </div>
                        <div class="card-footer">

                        </div>
                    </div>
                </div>
                <div class="col-12 col-md-6"></div>
            </div>
        </div>
    </div>

{% endblock %}